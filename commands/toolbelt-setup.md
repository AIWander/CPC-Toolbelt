---
name: toolbelt-setup
description: Configure CPC_ROOT, CPC_CACHE, Hands MCP, and optional Voice/Workflow/Manager
---

Run the `toolbelt-setup` skill. Ask for CPC_ROOT and CPC_CACHE (suggest `C:\CPC\cache`). Wire Hands only; enable optionals only if the user opts in. Do not add Programmer unless asked.
